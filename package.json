{
  "name": "omnidash",
  "version": "0.1.0",
  "private": true,
  "description": "OmniDash - Unified operator layer for Telegram + Discord + TikTok (monorepo)",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=frontend",
    "build": "npm run build --workspace=frontend",
    "lint": "npm run lint --workspace=frontend",
    "preview": "npm run preview --workspace=frontend",
    "install:dash": "npm install --workspace=frontend",
    "install:all": "npm install",
    "start:full": "echo 'Run platforms separately via their docker-compose or start scripts, then npm run dev for the dash'"
  }
}
