{
  "name": "@omnibox/core",
  "version": "0.1.0",
  "private": true,
  "description": "OmniBox shared core — canonical Omni types, the PlatformTransformer contract, and the CloudPhoneTransport contract. Imported by the shell, the platform engines, and the transports.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./cloud-phone": "./src/cloud-phone.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
